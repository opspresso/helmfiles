repositories:
  - name: argo
    url: https://argoproj.github.io/argo-helm

environments:
  default:
    values:
      - ../../environments/default.yaml
  eks-demo:
    values:
      - ../../environments/eks-demo.yaml

releases:
  - name: ns-argo
    namespace: default
    chart: incubator/raw
    version: ~0.1.0
    installed: true
    wait: true
    # force: true
    values:
      - resources:
          - apiVersion: v1
            kind: Namespace
            metadata:
              name: argo

  - name: argo
    namespace: argo
    chart: argo/argo
    version: ~0.6.5
    installed: true
    # wait: true
    values:
      - argo.yaml.gotmpl
