environments:
  default:
    values:
      - ../../environments/default.yaml
  eks-demo:
    values:
      - ../../environments/eks-demo.yaml

releases:
  - name: istio
    namespace: istio-system
    chart: incubator/raw
    version: ~0.2.3
    installed: true
    # wait: true
    # force: true
    # recreatePods: true
    values:
      - istio-demo.yaml
