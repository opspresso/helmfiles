repositories:
  - name: codecentric
    url: https://codecentric.github.io/helm-charts

environments:
  default:
    values:
      - ../../environments/default.yaml
  eks-demo:
    values:
      - ../../environments/eks-demo.yaml

releases:
  - name: keycloak
    namespace: keycloak
    chart: codecentric/keycloak
    version: ~7.5.0
    installed: true
    # wait: true
    # hooks:
    #   - events: ["prepare"]
    #     showlogs: true
    #     command: "/bin/sh"
    #     args:
    #       - -c
    #       - kubectl create secret -n keycloak generic realm-demo-secret --from-file=realm/demo.json
    values:
      - keycloak.yaml.gotmpl
