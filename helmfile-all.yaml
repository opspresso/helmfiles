# Ordered list of releases.

environments:
  default:
    values:
      - environments/default.yaml
  eks-demo:
    values:
      - environments/eks-demo.yaml

helmfiles:
  # kube-ingress
  - releases/cert-manager.yaml
  - releases/nginx-ingress.yaml

  # kube-system
  - releases/kube2iam.yaml
  - releases/metrics-server.yaml
  - releases/kube-state-metrics.yaml
  # - releases/cluster-autoscaler.yaml
  # - releases/cluster-overprovisioner.yaml
  # - releases/efs-provisioner.yaml

  # argo
  - releases/argo.yaml
  - releases/argo-cd.yaml
  # - releases/argo-events.yaml
  - releases/argo-rollouts.yaml

  # monitor
  - releases/prometheus.yaml
  - releases/prometheus-adapter.yaml
  - releases/grafana.yaml
